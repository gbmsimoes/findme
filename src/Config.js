import firebase from 'firebase';

// Config Firebase
export const initFirebase = () => {

    const config = {
        apiKey: "SSSSS",
        authDomain: "sssss",
        databaseURL: "sssss",
        projectId: "fsss",
        storageBucket: "",
        messagingSenderId: "8sssss"
    };
    firebase.initializeApp(config);
};